import { DateReferenciaPipe } from './date-referencia.pipe';

describe('DateReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new DateReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
