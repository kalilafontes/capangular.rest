import { SepararPalavrasPipe } from './separar-palavras.pipe';

describe('SepararPalavrasPipe', () => {
  it('create an instance', () => {
    const pipe = new SepararPalavrasPipe();
    expect(pipe).toBeTruthy();
  });
});
